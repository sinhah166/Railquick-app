## Git Operations
- **No Automatic Pushes:** Do not automatically run `git push` or push code to remote repositories unless the user explicitly requests it. You may stage and commit code if appropriate, but pushing must always require explicit user consent.
